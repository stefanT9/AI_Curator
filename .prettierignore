# Build output
.next/
out/
build/
coverage/

# Dependencies
node_modules/
package-lock.json

# Generated — owned by `supabase gen types`
src/types/database.ts

# Generated — owned by `scripts/build-corpus.ts`, which writes its own
# stable key order so a regeneration diffs as content, not a reshuffle.
supabase/seed-assets/corpus.json

# Local Supabase scratch
supabase/.temp/

# Vendored agent tooling — not ours to format
.agents/
.claude/

# 10x workflow docs — owned by the /10x-* skills
context/
